{% extends 'base.html.twig' %}

{% block title %} {{ post.title }} {% endblock %}

{% block content %}
    <div class="container">
        <h1>{{ post.title }}</h1>
        <p>{{ post.content }}</p>

        <hr>
        <h2>Commentaire</h2>
        <div class="">
            {% include "comment/_form.html.twig" %}
        </div>
        <hr>
        {% include "comment/_show.html.twig" %}

        <div class="navigation justify-content-center">
            {{ knp_pagination_render(comments, '@KnpPaginator/Pagination/bootstrap_v5_pagination.html.twig') }}
        </div>
    </div>


{% endblock %}