<form method="post" action="{{ path('app_link_media_delete', {'id': link_media.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ link_media.id) }}">
    <button class="btn">Delete</button>
</form>
